spring:
  application:
    name: nacos-learn
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        file-extension: yml
        encode: UTF-8
        prefix: jdbc
        group: test
        extension-configs:
          - data-id: redis.yml
            group: test
            refresh: true
          - data-id: db.yml
            group: test
            refresh: true
oss-upload:
    key: mykey
    value: 1212

